# Дополненная реальность в компьютерном зрении

Автор: Марьяновский Владислав Андреевич  
Группа: 292405-1

Учебный Unity проект для домашнего задания по предмету "Дополненная реальность в компьютерном зрении".

Версия Unity: 2020.3.15f2

## Домашнее задание 2

В проект добавлен бесплатный пакет Robot Kyle | URP из Unity Asset Store.

В сцене `SampleScene` размещены:

- `Robot Kyle Prefab`
- `Robot Kyle Prefab Variant`

Prefab и Prefab Variant находятся в папке `Assets/Prefabs`.

## Домашнее задание 3

В проект добавлен компонент `HomeworkFieldsAndSpawner`.

В компоненте есть:

- поля базовых типов
- поле с коллекцией
- ссылка на другой компонент `HomeworkDataTarget`
- ссылка на Prefab
- логика размещения Prefab после первого кадра

Компоненты размещены на сцене `SampleScene`.

## Домашнее задание 4

В проект добавлены компоненты:

- `KeyboardModelController`
- `CameraLookAtObject`

На сцене `SampleScene` модель `Robot Kyle Prefab` управляется клавишами:

- `A` и `D` вращают модель
- `W` и `S` двигают модель вперед и назад
- `C` меняет цвет модели

Камера `Main Camera` всегда смотрит на управляемую модель.

## Домашнее задание 5

В проект добавлена физическая сцена с Capsule-персонажем.

На сцене `SampleScene` есть:

- `Physics Homework Player` с `Rigidbody`, `CapsuleCollider` и управлением WASD
- `ThirdPersonCameraFollow` на `Main Camera`
- пол и стены с коллайдерами
- Prefab монетки `Coin`
- несколько монеток на сцене

При столкновении персонажа с монеткой она удаляется, а в консоль выводится сообщение `Монетка собрана!`.
